#include "user.h"

int main() {

    // hello();

    // helloYou("Mradul");

    // printf(getNumProc(), "Number of processes: %d\n");

    // printf(getMaxPid(), "Maximum pid: %d\n");

    return 0;
}